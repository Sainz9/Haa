import 'package:flutter/material.dart';

class MovieSpecial extends StatelessWidget {
  const MovieSpecial({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}